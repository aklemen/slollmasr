# Run 09: SlovenianGPT Transfer (Phase 5)
# Test best config with SlovenianGPT instead of GaMS-9B
# TODO: Set ALL params based on Phase 2-4 results
export RUN_NAME="run09_sloveniangpt"
export FREEZE_ENCODER=true  # UPDATE based on Phase 2 results
export ADAPTER_SUBSAMPLING=8  # UPDATE based on Phase 3 results
export ADAPTER_N_LAYERS=2  # UPDATE based on Phase 3 results
export ADAPTER_D_MODEL=1024  # UPDATE based on Phase 3 results
export LORA_RANK=128  # UPDATE based on Phase 4 results
export LORA_ALPHA=256
export LORA_TARGET_MODULES='["q_proj", "v_proj"]'  # UPDATE based on Phase 4 results
export LLM_NAME="aklemen/SlovenianGPT"
export PROMPT_FORMAT="mistral"
export WANDB_GROUP="phase5-transfer"
export WANDB_TAGS="freeze_encoder:true,subsampling:8,lora_r:128,lora_modules:qv,llm:sloveniangpt"
